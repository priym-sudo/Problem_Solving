if(head != null){
            SinglyLinkedListNode temp;
            temp = head;
            if(position == 0)
                head = head.next;
            else {
                for(int i=0; i<position-1; i++)
                    temp = temp.next;
                temp.next = (temp.next).next;
            }
        }
        return head;
