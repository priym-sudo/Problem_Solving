 int count=0;
        Arrays.sort(ar);
        int total = 0;
        for (int i = 0; i < n - 1; i++) {
            if (i < n && ar[i] == ar[i + 1]) {
                total++;
                i = i + 1;
            }
        }
        return total;
